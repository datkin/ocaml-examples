(executables (
  (names (inline_test_runner))
  (libraries (
    core
    example_lib
  ))
))

;(alias (
;  (name DEFAULT)
;  (deps (inline_test_runner.exe))
;  (run "./inline_test_runner.exe inline-test-runner example_lib")
;))
